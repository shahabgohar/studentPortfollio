export type Experience = {
    endDate: string;
    startDate: string;
    companyName: string;
    designation: string;
    roleSummary: string;
}

export type Skill = {
    firstName: string;
    lastName: string;
    description: string;
}