{
  "name": "portfollio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build && nuxt generate",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.14.0",
    "nuxt-compress": "^3.0.0"
  },
  "devDependencies": {
    "@nuxtjs/svg": "^0.1.12",
    "node-sass": "^4.14.1",
    "sass-loader": "^10.0.1"
  }
}
